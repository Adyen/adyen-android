plugins {
    alias libs.plugins.checkout.android.library
    alias libs.plugins.kotlin.parcelize
}

checkoutPublishing {
    id = "cse"
    name = "Adyen Client Side Encryption"
    description = "Adyen Client Side Encryption for Adyen's Checkout API."
}

android {
    namespace 'com.adyen.checkout.cse'

    testFixtures {
        enable = true
    }
}

dependencies {
    // Checkout
    api project(':checkout-core')

    //Tests
    testImplementation libs.json
    testImplementation libs.bundles.junit
    testImplementation libs.bundles.mockito
    testImplementation libs.jose4j

    testFixturesImplementation libs.bundles.junit
    testFixturesImplementation libs.bundles.mockito
}
