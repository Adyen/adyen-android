plugins {
    alias libs.plugins.checkout.android.library
    alias libs.plugins.kotlin.parcelize
}

checkoutPublishing {
    id = "convenience-stores-jp"
    name = "Adyen Japanese Convenience Stores Component"
    description = "Adyen checkout Japanese Convenience Stores Component client for Adyen's Checkout API."
}

android {
    namespace 'com.adyen.checkout.conveniencestoresjp'

    buildFeatures {
        viewBinding true
    }
}

dependencies {
    api project(':econtext')

    testImplementation testFixtures(project(':test-core'))
    testImplementation libs.bundles.junit
    testImplementation libs.bundles.mockito
}
