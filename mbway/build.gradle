/*
 * Copyright (c) 2020 Adyen N.V.
 *
 * This file is open source and available under the MIT license. See the LICENSE file for more info.
 *
 * Created by caiof on 17/12/2020.
 */

plugins {
    alias libs.plugins.checkout.library
    alias libs.plugins.kotlin.compose
    alias libs.plugins.kotlin.parcelize
}

checkoutPublishing {
    id = "mbway"
    name = "Adyen checkout MB WAY component"
    description = "Adyen checkout MB WAY component client for Adyen's Checkout API."
}

android {
    namespace 'com.adyen.checkout.mbway'

    // TODO - remove view binding after old component is removed
    buildFeatures {
        viewBinding true
    }
}

dependencies {
    // Checkout
    api project(':core')
    api project(':action-core')
    api project(':ui-core')
    api project(':sessions-core')

    // Dependencies
    implementation libs.androidx.startup
    implementation libs.material

    //Tests
    testImplementation testFixtures(project(':core'))
    testImplementation testFixtures(project(':test-core'))
    testImplementation testFixtures(project(':components-core'))
    testImplementation testFixtures(project(':ui-core'))
    testImplementation libs.bundles.junit
    testImplementation libs.bundles.kotlin.coroutines.test
    testImplementation libs.bundles.mockito
}
