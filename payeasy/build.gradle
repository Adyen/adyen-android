plugins {
    alias libs.plugins.checkout.android.library
    alias libs.plugins.kotlin.parcelize
}

checkoutPublishing {
    id = "payeasy"
    name = "Adyen Pay Easy Component"
    description = "Adyen checkout Pay Easy Component client for Adyen's Checkout API."
}

android {
    namespace 'com.adyen.checkout.payeasy'

    buildFeatures {
        viewBinding true
    }
}

dependencies {
    api project(':econtext')

    testImplementation libs.bundles.junit
    testImplementation libs.bundles.mockito
}
