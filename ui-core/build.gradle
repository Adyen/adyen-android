/*
 * Copyright (c) 2019 Adyen N.V.
 *
 * This file is open source and available under the MIT license. See the LICENSE file for more info.
 *
 * Created by arman on 15/2/2019.
 */

plugins {
    alias libs.plugins.checkout.android.library
    alias libs.plugins.kotlin.parcelize
}

checkoutPublishing {
    id = "ui-core"
    name = "Adyen checkout base UI component"
    description = "Adyen checkout base UI component client for Adyen's Checkout API."
}

android {
    namespace 'com.adyen.checkout.ui.core'

    buildFeatures {
        viewBinding true
    }

    testFixtures {
        enable = true
    }
}

dependencies {
    // Checkout
    api project(':components-core')

    // Dependencies
    implementation libs.androidx.appcompat
    api libs.androidx.autofill
    api libs.androidx.constraintlayout
    implementation libs.androidx.recyclerview
    api libs.material
    implementation libs.androidx.browser

    //Tests
    testImplementation libs.json
    testImplementation libs.bundles.junit
    testImplementation libs.bundles.mockito
    testImplementation libs.bundles.kotlin.coroutines.test
    testImplementation libs.robolectric
    testImplementation libs.bundles.mockito
}
